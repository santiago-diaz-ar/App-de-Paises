const { Country } = require("../db");

const createCountry = async (
  name,
  currencies,
  capital,
  región,
  languages,
  área,
  population,
  continents,
  maps,
  flags
) => {
  return true;
};

module.exports = createCountry;
